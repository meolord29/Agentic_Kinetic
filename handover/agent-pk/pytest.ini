[pytest]
pythonpath = src
testpaths = tests
markers =
    slow: runs at the committed demonstration settings rather than the reduced ones the
        structural tests use. Deselect with -m "not slow" while iterating; CI runs everything.
