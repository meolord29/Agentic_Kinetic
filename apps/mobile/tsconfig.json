{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      // whisper.rn's exports map lacks a "." entry — point TS at its bundled
      // types directly (see src/voice/whisper-rn.d.ts).
      "whisper.rn": ["../../node_modules/whisper.rn/lib/typescript/index.d.ts"]
    }
  }
}
